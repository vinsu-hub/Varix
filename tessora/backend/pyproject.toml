[project]
name = "tessora-backend"
version = "0.1.0"
description = "Tessora - Enterprise Document Intelligence API"
requires-python = ">=3.11"

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
